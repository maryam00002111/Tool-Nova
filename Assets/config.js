// ============================================================
// TOOLNOVA — site configuration
// Fill these in once you've created the real GitHub repo and connected
// it to Netlify. Until then, blog.html and blog-post.html will show
// a "content not connected yet" message instead of failing silently.
// ============================================================

const SITE_CONFIG = {
  githubOwner: "",   // e.g. "maryam-dev" — your GitHub username or org
  githubRepo: "",    // e.g. "toolnova"   — the repo name
  githubBranch: "main"
};
